"""
Before running the first version of your application, Flask needs to be told how
to import it  by setting the FLASK_APP environment variable:
`(venv) $ set FLASK_APP=miniblog.py` // Microsoft Windows
`(venv) $ export FLASK_APP=miniblog` // Linux or Mac OS
Then run your first application:
`(venv) $ flask run` // view your application on local browser
 """